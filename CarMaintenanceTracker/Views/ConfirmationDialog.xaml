<Window x:Class="CarMaintenanceTracker.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="" Height="Auto" Width="Auto"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="{StaticResource AppFontFamily}">
    <Border Style="{StaticResource PanelCardStyle}" MinWidth="320" MaxWidth="420" Margin="12">
        <StackPanel>
            <TextBlock x:Name="MessageTextBlock"
                       Style="{StaticResource CardTitleTextStyle}"
                       TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="CancelButton" Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0" Click="CancelButton_Click" />
                <Button x:Name="ConfirmButton" Content="Confirm"
                        Click="ConfirmButton_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
