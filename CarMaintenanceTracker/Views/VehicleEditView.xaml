<UserControl x:Class="CarMaintenanceTracker.Views.VehicleEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:CarMaintenanceTracker.Converters"
             xmlns:behaviors="clr-namespace:CarMaintenanceTracker.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:NotNullToBooleanConverter x:Key="NotNullToBooleanConverter" />
    </UserControl.Resources>
    <StackPanel Margin="0,12,0,0">
        <TextBlock Text="Edit Vehicle" Style="{StaticResource CardTitleTextStyle}" Margin="0,0,0,8" />

        <Grid IsEnabled="{Binding SelectedVehicle, Converter={StaticResource NotNullToBooleanConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Nickname" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,3"
                     Text="{Binding SelectedVehicle.Nickname, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Make" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,3"
                     Text="{Binding SelectedVehicle.Make, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Model" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,3"
                     Text="{Binding SelectedVehicle.Model, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Year" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,3"
                     behaviors:NumericInputBehavior.Mode="Integer"
                     behaviors:NumericInputBehavior.MaxLength="4"
                     Text="{Binding SelectedVehicle.Year, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Plate" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox Grid.Row="4" Grid.Column="1" Margin="0,3"
                     Text="{Binding SelectedVehicle.Plate, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />
        </Grid>

        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Add New" Command="{Binding AddVehicleCommand}" Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}" />
            <Button Content="Save" Command="{Binding SaveVehicleCommand}" Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="{Binding SelectedVehicle.HasErrors, Converter={StaticResource InverseBooleanConverter}, FallbackValue=False}" />
            <Button Content="Delete" Click="DeleteButton_Click"
                    Style="{StaticResource DeleteButtonStyle}"
                    IsEnabled="{Binding SelectedVehicle, Converter={StaticResource NotNullToBooleanConverter}}" />
        </StackPanel>
    </StackPanel>
</UserControl>
